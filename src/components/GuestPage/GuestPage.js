import classes from './GuestPage.module.css';
const GuestPage = () => {
  return (
    <section className={classes.starting}>
      <h1>Welcome to React Authentication!</h1>
    </section>
  );
};

export default GuestPage;
